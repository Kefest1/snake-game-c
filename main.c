#include "snake.h"

int main(void) {
    /*initscr();
    attron(A_REVERSE);
    mvprintw(5, 5, "Test");
    refresh();
    attroff(A_REVERSE);
    mvprintw(7, 5, "%d", getch());
    sleep(2u);
    endwin();*/
    startGame();
    return 0;
}
